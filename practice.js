for(let num=10; num>0; num--) {
    console.log(`Your number is ${num}`);
}; 
/*function Car(make, model, year, color, passengers, convertible, mileage, started) {
    this.make = make;
    this.model = model;
    this.year = year;
    this.color = color;
    this.passengers = passengers;
    this.convertible = convertible;
    this.mileage = mileage;
    this.started = started;

    start: function() { this.started = true; },

    stop: function() { this.started = false; }, 

    drive: function() {
        if (this.started) {
            console.log( this.make + " " + this.model + " goes zoom zoom!" );
        } else { console.log('Start the engine first!'); }
    },
};
//so instead of writng out a new object for every car, an object constructor is like a factory that can mass produce using the 'this' and 'new' keyword
var chevy = new Car("Chevy", "Bel Air", "1957", "red", "2", false, "1021", false);
var fiat = new Car("Fiat", "500", "1957", "Medium Blue", "2", false, "88,000", false);
var cadi = new Car("GM", "Cadillac", "1955", "tan", "5", false, "12,892", false);
var taxi = new Car("Webville Motors", "Taxi", "1955", "yellow", "4", false, "281341", false);
*/
